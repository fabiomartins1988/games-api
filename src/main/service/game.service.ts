import { IGame } from "../infrastructure/repository/interface/game.interface";

// export const findAll = async (): Promise<IGame[]> => Object.values(arrayDoJson);
// export const find = async (id: number): Promise<IGame> => arrayDoJson[id];
// Create,
// Update,
// Delete.
 
export class UserService {

    public async teste() {
        
    }
}